---
const projects = [
    {
        title: "Plataforma SaaS de Gestión",
        description:
            "Sistema integral de gestión empresarial con arquitectura microservicios.",
        tech: ["Laravel", "Vue.js", "Docker"],
        link: "#",
    },
    {
        title: "Bot de Automatización IA",
        description:
            "Bot inteligente para procesamiento de documentos legales usando NLP.",
        tech: ["Python", "OpenAI API", "AWS Lambda"],
        link: "#",
    },
    {
        title: "API Segura Fintech",
        description:
            "API RESTful para transacciones financieras con altos estándares de seguridad.",
        tech: ["Node.js", "PostgreSQL", "Redis"],
        link: "#",
    },
];
---

<section id="projects" class="py-20">
    <h2 class="text-3xl font-bold mb-12 flex items-center gap-4">
        <span class="text-cyan-400">01.</span> Proyectos Destacados
        <span class="h-px bg-slate-700 flex-grow max-w-xs"></span>
    </h2>

    <div class="grid md:grid-cols-3 gap-8">
        {
            projects.map((project) => (
                <div class="bg-slate-900 rounded-xl overflow-hidden border border-slate-800 hover:border-cyan-500/30 transition-all hover:-translate-y-2 group">
                    <div class="h-48 bg-slate-800 flex items-center justify-center group-hover:bg-slate-800/80 transition-colors">
                        <span class="text-slate-500 text-4xl">📂</span>
                    </div>
                    <div class="p-6 space-y-4">
                        <h3 class="text-xl font-bold text-slate-100 group-hover:text-cyan-400 transition-colors">
                            {project.title}
                        </h3>
                        <p class="text-slate-400 text-sm leading-relaxed">
                            {project.description}
                        </p>
                        <div class="flex flex-wrap gap-2 pt-2">
                            {project.tech.map((t) => (
                                <span class="text-xs font-mono text-cyan-400 bg-cyan-950/30 px-2 py-1 rounded">
                                    {t}
                                </span>
                            ))}
                        </div>
                    </div>
                </div>
            ))
        }
    </div>
</section>
